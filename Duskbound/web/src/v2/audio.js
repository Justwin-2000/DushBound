export class Audio {
 constructor(){this.volume=.65;this.voices=0;this.ctx=null;}
 unlock(){try{if(!this.ctx)this.ctx=new(window.AudioContext||window.webkitAudioContext)();if(this.ctx.state==='suspended')this.ctx.resume().catch(()=>{});}catch{}}
 suspend(){try{this.ctx?.suspend().catch(()=>{});}catch{}}
 play(kind,material='mist'){const c=this.ctx;if(!c||c.state!=='running'||!this.volume||this.voices>=6)return;this.voices++;try{const now=c.currentTime,v=this.volume*.07,g=c.createGain(),o=c.createOscillator();const frequency={P01:760,S01:110,M01:390,W01:900,dash:250,skill:520,hit:material==='metal'?1400:material==='wood'?200:80,hurt:75,swap:430}[kind]||440;const length=kind==='skill'?.55:kind==='W01'?.23:kind==='S01'?.18:.10;o.type=kind==='W01'||kind==='skill'?'sine':kind==='S01'?'sawtooth':'triangle';o.frequency.setValueAtTime(frequency*(.97+Math.random()*.06),now);o.frequency.exponentialRampToValueAtTime(Math.max(40,frequency*(kind==='W01'?1.5:.3)),now+length);g.gain.setValueAtTime(.001,now);g.gain.exponentialRampToValueAtTime(v,now+.008);g.gain.exponentialRampToValueAtTime(.001,now+length);o.connect(g);g.connect(c.destination);o.start();o.stop(now+length+.01);o.onended=()=>{o.disconnect();g.disconnect();this.voices--;};if(['P01','S01','M01'].includes(kind)){if(!this.noise){this.noise=c.createBuffer(1,Math.floor(c.sampleRate*.2),c.sampleRate);const data=this.noise.getChannelData(0);for(let i=0;i<data.length;i++)data[i]=Math.random()*2-1;}const n=c.createBufferSource(),filter=c.createBiquadFilter(),gain=c.createGain();n.buffer=this.noise;filter.type='bandpass';filter.frequency.setValueAtTime(kind==='M01'?1600:2400,now);filter.frequency.exponentialRampToValueAtTime(kind==='S01'?90:430,now+.1);gain.gain.setValueAtTime(v*(kind==='S01'?3:1.5),now);gain.gain.exponentialRampToValueAtTime(.001,now+.12);n.connect(filter);filter.connect(gain);gain.connect(c.destination);n.start();n.stop(now+.13);n.onended=()=>{n.disconnect();filter.disconnect();gain.disconnect();};}}catch{this.voices=Math.max(0,this.voices-1);}}
}
