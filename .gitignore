# 仓库根在 Dush/，而 Duskbound/.gitignore 只覆盖项目内部，
# 所以这些“不该进版本库”的东西必须在这里排除。

# ── 安卓构建工具链（约 555 MB / 11800+ 文件）─────────────────
# 由 android/install-toolchain.ps1 从官方下载并校验固定哈希后生成，可随时重建。
.tooling/

# ── 一次性备份目录 ─────────────────────────────────────────
# 用版本控制接管前的临时快照，第一次干净提交后即可删除。
_backup-before-batch1/

# ── 压缩包与重复素材副本 ───────────────────────────────────
*.rar
*.7z
*.zip
Duskbound-assets/

# ── 系统与编辑器杂项 ──────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.swp
.vscode/
.idea/
