# 项目协作要求

## 每次版本更新同步 README

用户明确要求：后续每次更新版本时，都要同步更新 README 并上传远程仓库。

- 仓库首页 `README.md` 面向当前版本，项目说明 `Duskbound/README.md` 保持一致。
- 改动版本号或交付新版本时，更新版本号、本次功能与修复、安装与构建产物路径、存档兼容性、实际验证结果及尚未完成的内容。
- 文档和对应实现放在同一次版本提交中；上传版本时同时上传 README。保留历史迭代记录，但明确区分当前功能与历史功能。
- 只记录已执行的验证，不把浏览器模拟写成安卓真机实测，不把规划数据写成已完成玩法。
- 发布前核对 README 版本与 `Duskbound/package.json`、Android Manifest 一致，并检查文档链接；没有上传的安装包不能写成已有远程下载附件。
- 纯文档更新无需递增应用版本号或重新编译 APK。
